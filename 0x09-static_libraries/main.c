#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
char *str;

str = "\"At the end of the day, my goal was to be the best hacker \"\n\t- Kevin Mitnick";
_puts(str);
return (0);
}
